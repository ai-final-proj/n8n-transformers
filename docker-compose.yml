services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    environment:
      - DATABASE_URL=postgresql+psycopg://neondb_owner:npg_MPZ3lB5RtSve@ep-green-truth-adedoaj5-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=localhost
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=neondb
      - DB_POSTGRESDB_USER=neondbowner
      - DB_POSTGRESDB_PASSWORD=npg_0TbRIMg4nqQo@ep
      - NODE_OPTIONS=--max-old-space-size=6144
    volumes:
      - ./n8n_data:/home/node/.n8n
    networks:
      - n8n-net

#  ollama:
#    image: ollama/ollama:latest
#    restart: always
#    ports:
#      - "11434:11434"
#    volumes:
#      - ollama_data:/root/.ollama
#    networks:
#      - n8n-net
#
#  huggingface:
#    image: ghcr.io/huggingface/text-generation-inference:latest
#    platform: linux/amd64
#    restart: always
#    ports:
#      - "8080:80"
#    volumes:
#      - huggingface_data:/data
#    networks:
#      - n8n-net

#volumes:
#  ollama_data:
#  huggingface_data:

networks:
  n8n-net:
    driver: bridge
